To create an environment from an environment.yml file
Use the terminal : conda env create -f environment.yml



To import the required librairies using requierements.txt
Use the terminal : pip install -r requirements.txt
